{
  "name": "yandex-reviews-service-root",
  "version": "1.0.0",
  "private": true,
  "description": "Сервис сбора отзывов и карточек организаций с Яндекс.Карт (Laravel + Vue 3 SPA)",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && php artisan serve",
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "setup:backend": "cd backend && composer install && php artisan key:generate && php artisan migrate",
    "setup:frontend": "cd frontend && npm install",
    "setup": "npm run setup:backend && npm run setup:frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
